## 1.0.0

- Initial release of `struct_generator`
- Added support for generating page structure
- Added `-o` option to specify output directory
- Automatic folder creation inside `lib/`
- Generates page class with template
- Provides CLI usage via `struct_generator show page <name>`
